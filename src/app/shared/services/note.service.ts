import { Injectable } from '@angular/core';
import {FormBuilder, FormGroup, Validators} from '@angular/forms';
// import {NgbModal} from '@ngx-bootstrap';

import {NotesComponent, iNote} from '../../components/notes/notes.component';

@Injectable({
  providedIn: 'root'
})
export class NoteService {

  constructor() { }
}
